{# def gp, msp #}

<IValueList>
    {%- for dp in array(gp.results) %}
        <IField label={ dp.name } >
            <mst.MBDB.MeasuredValue value={dp} />
        </IField>
    {% endfor %}
</IValueList>
