{# def record, metadata, ui, extra_context, d, context #}

{% set md=d.metadata %}
{% set gp=md.general_parameters %}
{% set msp=md.method_specific_parameters %}

<mst.MBDB.RecordHeader md={md} links={d.links} />

{#
    note: if you do not specify label for IField, it is taken directly from the model
    (and translated to the current language) and thus is consistent with facets
#}

<IValueList>
    <IField d={gp.technique} field_class="mbdbv-record-technique" />
    <IField d={gp.depositors} field_class="mbdbv-record-technique">
        <mst.MBDB.Depositors depositors={gp.depositors} />
    </IField>
</IValueList>

{#        <IField label="Main Publication:" #}
{#                d={gp.associated_publication}#}
{#                field_class="mbdbv-record-technique">#}
{#            <AssociatedPublication d={gp.associated_publication} />#}
{#        </IField>#}


<div class="mbdbv-record-container">

    <mst.Common.Section title="Entities of interest">
        <mst.MBDB.EntitiesOfInterest entities={gp.entities_of_interest} />
    </mst.Common.Section>

    <mst.Common.Section title="Results">
        <mst.MST.Results gp={gp} msp={msp} />
    </mst.Common.Section>

    <mst.Common.Section title="Measurements">
        <mst.MST.Measurements gp={gp} msp={msp} />
    </mst.Common.Section>

    <mst.Common.Section title="Instrument">
        <mst.MBDB.Instrument instrument={gp.instrument} />
    </mst.Common.Section>

    <mst.Common.Section title="Raw measurements" >
        <mst.MBDB.RawMeasurements raw_measurements={gp.raw_measurements} files={extra_context.files} />
    </mst.Common.Section>

</div>
