{# def gp, msp #}

<IValueList>
    {%- for dp in array(gp.results) %}
        <IField label={ dp.name } >
            <bli.MBDB.MeasuredValue value={dp} />
        </IField>
    {% endfor %}
</IValueList>
