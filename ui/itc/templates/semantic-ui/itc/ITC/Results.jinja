{# def gp, msp #}

<IValueList>
    {%- for dp in array(gp.results) %}
        <IField label={ dp.name } >
            <itc.MBDB.MeasuredValue value={dp} />
        </IField>
    {% endfor %}
</IValueList>
