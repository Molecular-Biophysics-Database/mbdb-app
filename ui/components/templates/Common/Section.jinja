{# def title=None #}

<div class="bg-primary rounded-normal p-6 h-fit mb-4 last:mb-0">
  {% if title %}<h3 class="mbdbv-record-section-title font-JostMedium text-26px mb-8">
    {{ title }}</h3>{%
  endif %}
  <div class="mbdbv-name-value-list">
    {{ content }}
  </div>
</div>