{# def self_link, state #}
{%- block page_body %}
    {% if state._ui_value in ("draft", "submitted", "accepted") %}
        <div>
            <div id="delete-button" data-selflink="{{self_link}}"></div>
        </div>
    {% endif %}
{%- endblock page_body %}