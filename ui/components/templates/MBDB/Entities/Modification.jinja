{# def mod #}

{# Recordview template that renders modifications (e.g. of Polymer objects) #}

<IField label="Type" d={mod.type} />
<IField label="Position" d={mod.position} />
<IField label="Protocols" d={mod.protocol}>
    <Protocol title="Protocol step" protocols={mod.protocol} />
</IField>