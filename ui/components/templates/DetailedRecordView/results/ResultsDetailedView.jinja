{# def results #}

{% for result in array(results, remove_empty=True) %}
{% set title='Result ' ~ loop.index %}
<div class="mb-4 pb-8 border-b-[.1px] border-b-dark last:border-b-0 last:mb-0">
    <SubSection title={ title }>
        <ResultDetailedView result={result} />
    </SubSection>
</div>
{% endfor %}